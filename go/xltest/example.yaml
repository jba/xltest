# A simple example test file.
# The function under test returns the sum of two integers.
name: add
description: test basic functionality
subtests:
  - in: [0, 0]
    want: 0
  - {"in": [1, 2], "want": 3}
  - {
      in: [-2, 1], 
      "want": -1
    }
