{
  "name": "env",
  "description": "test getting an environment variable",
  "functions": {
    "lookup": "takes a string, returns it if non-empty, else returns env var XLTEST"
  },
  "env": {"XLTEST": "bar"},
  "subtests": [
    {"call": ["lookup", "foo"], "want": "foo"},
    {"call": ["lookup", ""], "want": "bar"}
  ]
}

