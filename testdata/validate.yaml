# test function: simulate LLM output using input as prompt.
# The function should return a string that contains the prompt.
# validate function: partial regexp match.

name: validate
description: tests a validate function 
in: Describe a cat.
want: cat
